import React from 'react';
import { RotateCcw, Search } from 'lucide-react';

const control = 'min-w-0 rounded-xl border border-zinc-200 bg-zinc-50 px-3 py-2.5 text-xs text-zinc-700 outline-none focus:border-emerald-500 focus:ring-2 focus:ring-emerald-500/15 dark:border-zinc-800 dark:bg-zinc-950 dark:text-zinc-300';

export function AdminFilterBar({ draft, onDraftChange, onApply, onClear, selectValue, onSelectChange, selectLabel, options }) {
  return <form onSubmit={(event) => { event.preventDefault(); onApply(); }} className="mt-5 flex flex-col gap-2 sm:flex-row"><div className="relative min-w-0 flex-1"><Search aria-hidden="true" className="pointer-events-none absolute left-3 top-1/2 h-3.5 w-3.5 -translate-y-1/2 text-zinc-400" /><input aria-label={selectLabel ? `Buscar e filtrar ${selectLabel.toLowerCase()}` : 'Buscar'} name="admin-search" value={draft} onChange={(event) => onDraftChange(event.target.value)} placeholder="Buscar por texto, título ou autor…" className={`${control} w-full pl-9`} /></div>{selectLabel && <select aria-label={selectLabel} value={selectValue} onChange={(event) => onSelectChange(event.target.value)} className={`${control} sm:w-32`}>{options.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select>}<button type="submit" className="rounded-xl bg-emerald-500 px-3.5 py-2.5 text-xs font-bold text-zinc-950 transition-colors hover:bg-emerald-400 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-emerald-500 focus-visible:ring-offset-2 dark:focus-visible:ring-offset-zinc-900">Filtrar</button>{(draft || selectValue) && <button type="button" onClick={onClear} aria-label="Limpar filtros" className="inline-flex items-center justify-center rounded-xl border border-zinc-200 px-3 text-zinc-500 transition-colors hover:border-zinc-400 hover:text-zinc-800 dark:border-zinc-800 dark:hover:text-zinc-200"><RotateCcw className="h-3.5 w-3.5" /></button>}</form>;
}
